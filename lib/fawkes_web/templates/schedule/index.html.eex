<div>
  <%= link("Schedule", to: schedule_path(@conn, :index)) %> |
  <%= link("Agenda", to: agenda_item_path(@conn, :index)) %>
</div>

<%=
  render(FawkesWeb.ScheduleView, "schedule_slots.html",
                                 conn: @conn,
                                 schedules: @schedules,
                                 talk_counts: @talk_counts,
                                 agenda_item_slugs: @agenda_item_slugs)
%>