<div>
  <%= for slot <- @slots do %>
  <%=
    render_slot(slot, [
      slot: slot,
      conn: @conn,
      talk_counts: @talk_counts,
      agenda_item_slugs: @agenda_item_slugs
    ])
  %>
  <% end %>
</div>
